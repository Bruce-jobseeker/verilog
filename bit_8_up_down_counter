module bit_8_up_down_counter(
    input clk,
    input rst,
    input mode,
    output [7:0] cnt_out
    );
    
    reg [7:0] cnt_out;
    
    always@(posedge clk) begin
    if(rst)
    cnt_out<=0;
    else begin
    if(mode)
    cnt_out<=cnt_out+1;
    else if(!mode)
    cnt_out<=cnt_out-1;
    end
    end
    
endmodule
